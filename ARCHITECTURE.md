# Authentication Flow Diagram

## 🔄 Complete Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER AUTHENTICATION FLOW                      │
└─────────────────────────────────────────────────────────────────┘

1. USER REGISTRATION
   ┌──────────┐
   │  User    │ Fills signup form
   │ (Guest)  │────────────────────┐
   └──────────┘                    │
                                   ▼
                          ┌─────────────────┐
                          │   Auth.jsx      │
                          │  (Frontend)     │
                          └────────┬────────┘
                                   │ dispatch(register())
                                   ▼
                          ┌─────────────────┐
                          │  authSlice.js   │
                          │   (Redux)       │
                          └────────┬────────┘
                                   │ POST /api/users
                                   ▼
                          ┌─────────────────┐
                          │  userRoutes.js  │
                          │   (Backend)     │
                          └────────┬────────┘
                                   │
                                   ▼
                          ┌─────────────────┐
                          │ authController  │
                          │  .registerUser  │
                          └────────┬────────┘
                                   │
                                   ├─► Validate email format
                                   ├─► Validate password strength
                                   ├─► Validate phone number
                                   ├─► Check duplicate email
                                   ├─► Hash password (bcrypt)
                                   │
                                   ▼
                          ┌─────────────────┐
                          │   User Model    │
                          │   (MongoDB)     │
                          └────────┬────────┘
                                   │ Save user
                                   ▼
                          ┌─────────────────┐
                          │ Generate JWT    │
                          │   (30 days)     │
                          └────────┬────────┘
                                   │
                                   ▼
   ┌──────────┐          ┌─────────────────┐
   │  User    │◄─────────│  Return token   │
   │(Logged In)│          │  + user data    │
   └──────────┘          └─────────────────┘
        │
        └─► Token stored in localStorage
        └─► User data in Redux store
        └─► Redirected to home/checkout


2. USER LOGIN
   ┌──────────┐
   │  User    │ Enters credentials
   │ (Guest)  │────────────────────┐
   └──────────┘                    │
                                   ▼
                          ┌─────────────────┐
                          │   Auth.jsx      │
                          │  (Frontend)     │
                          └────────┬────────┘
                                   │ dispatch(login())
                                   ▼
                          ┌─────────────────┐
                          │  authSlice.js   │
                          │   (Redux)       │
                          └────────┬────────┘
                                   │ POST /api/users/login
                                   ▼
                          ┌─────────────────┐
                          │  userRoutes.js  │
                          │   (Backend)     │
                          └────────┬────────┘
                                   │
                                   ▼
                          ┌─────────────────┐
                          │ authController  │
                          │   .authUser     │
                          └────────┬────────┘
                                   │
                                   ├─► Validate email format
                                   ├─► Find user by email
                                   ├─► Compare password (bcrypt)
                                   │
                                   ▼
                          ┌─────────────────┐
                          │ Generate JWT    │
                          │   (30 days)     │
                          └────────┬────────┘
                                   │
                                   ▼
   ┌──────────┐          ┌─────────────────┐
   │  User    │◄─────────│  Return token   │
   │(Logged In)│          │  + user data    │
   └──────────┘          └─────────────────┘
        │
        └─► Token stored in localStorage
        └─► User data in Redux store
        └─► Redirected to previous page


3. ACCESSING PROTECTED ROUTE (e.g., /checkout)
   ┌──────────┐
   │  User    │ Clicks "Checkout"
   │          │────────────────────┐
   └──────────┘                    │
                                   ▼
                          ┌─────────────────┐
                          │ ProtectedRoute  │
                          │  (Component)    │
                          └────────┬────────┘
                                   │
                                   ├─► Check isAuthenticated
                                   │   (from Redux)
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
            ┌──────────────┐            ┌──────────────┐
            │ Authenticated│            │      Not     │
            │    = true    │            │Authenticated │
            └──────┬───────┘            └──────┬───────┘
                   │                           │
                   ▼                           ▼
            ┌──────────────┐            ┌──────────────┐
            │   Render     │            │  Redirect to │
            │   Checkout   │            │    /login    │
            └──────────────┘            └──────┬───────┘
                                               │
                                               └─► Save current URL
                                               └─► After login, redirect back


4. UPDATING PROFILE
   ┌──────────┐
   │  User    │ Updates profile
   │(Logged In)│────────────────────┐
   └──────────┘                     │
                                    ▼
                          ┌─────────────────┐
                          │ UserProfile.jsx │
                          │  (Frontend)     │
                          └────────┬────────┘
                                   │ dispatch(updateUserProfile())
                                   ▼
                          ┌─────────────────┐
                          │  authSlice.js   │
                          │   (Redux)       │
                          └────────┬────────┘
                                   │ PUT /api/users/profile
                                   │ Authorization: Bearer <token>
                                   ▼
                          ┌─────────────────┐
                          │  userRoutes.js  │
                          │   (Backend)     │
                          └────────┬────────┘
                                   │
                                   ▼
                          ┌─────────────────┐
                          │ authMiddleware  │
                          │   .protect      │
                          └────────┬────────┘
                                   │
                                   ├─► Extract token from header
                                   ├─► Verify JWT token
                                   ├─► Get user from database
                                   ├─► Attach user to req.user
                                   │
                                   ▼
                          ┌─────────────────┐
                          │ authController  │
                          │.updateUserProfile│
                          └────────┬────────┘
                                   │
                                   ├─► Validate new email
                                   ├─► Validate new phone
                                   ├─► Check email uniqueness
                                   ├─► Hash new password (if provided)
                                   ├─► Update user in database
                                   │
                                   ▼
                          ┌─────────────────┐
                          │ Generate new JWT│
                          │   (30 days)     │
                          └────────┬────────┘
                                   │
                                   ▼
   ┌──────────┐          ┌─────────────────┐
   │  User    │◄─────────│  Return token   │
   │(Updated) │          │  + updated data │
   └──────────┘          └─────────────────┘
        │
        └─► New token in localStorage
        └─► Updated user data in Redux


5. LOGOUT
   ┌──────────┐
   │  User    │ Clicks "Logout"
   │(Logged In)│────────────────────┐
   └──────────┘                     │
                                    ▼
                          ┌─────────────────┐
                          │   Navbar.jsx    │
                          │  (Frontend)     │
                          └────────┬────────┘
                                   │ dispatch(logout())
                                   ▼
                          ┌─────────────────┐
                          │  authSlice.js   │
                          │   (Redux)       │
                          └────────┬────────┘
                                   │
                                   ├─► Clear user from state
                                   ├─► Clear token from state
                                   ├─► Remove from localStorage
                                   │
                                   ▼
   ┌──────────┐          ┌─────────────────┐
   │  User    │◄─────────│  Redirect to    │
   │ (Guest)  │          │      home       │
   └──────────┘          └─────────────────┘
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                      SECURITY ARCHITECTURE                       │
└─────────────────────────────────────────────────────────────────┘

Frontend Security:
├─► Input Validation (client-side)
├─► Protected Routes (ProtectedRoute component)
├─► Token Storage (localStorage)
└─► Redux State Management

Backend Security:
├─► Input Sanitization (validation.js)
├─► Email Validation (regex)
├─► Phone Validation (regex)
├─► Password Strength (uppercase, lowercase, number)
├─► Password Hashing (bcrypt, 10 rounds)
├─► JWT Token Generation (30-day expiry)
├─► JWT Token Verification (authMiddleware)
├─► Protected Routes (protect middleware)
└─► Database Indexes (email uniqueness)

Database Security:
├─► Passwords stored as hashes (never plain text)
├─► Email stored as lowercase (normalization)
├─► Unique email constraint
└─► Timestamps for audit trail
```

## 📊 Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         DATA FLOW                                │
└─────────────────────────────────────────────────────────────────┘

User Input → Frontend Validation → Redux Action → API Call
                                                      ↓
                                            Backend Validation
                                                      ↓
                                            Database Operation
                                                      ↓
                                            Generate JWT
                                                      ↓
                                            Return Response
                                                      ↓
Redux Store Update ← localStorage Update ← Response Received
        ↓
   UI Update
```

## 🗄️ Database Schema

```
┌─────────────────────────────────────────────────────────────────┐
│                      MongoDB Collection: users                   │
└─────────────────────────────────────────────────────────────────┘

{
  _id: ObjectId("..."),
  name: "John Doe",
  email: "john@example.com",              // lowercase, unique
  password: "$2a$10$...",                  // bcrypt hash
  phone: "+91 9876543210",                 // optional
  isAdmin: false,                          // default: false
  createdAt: ISODate("2026-02-05T..."),   // auto
  updatedAt: ISODate("2026-02-05T...")    // auto
}

Indexes:
- _id (default)
- email (unique)
```

## 🎯 State Management

```
┌─────────────────────────────────────────────────────────────────┐
│                    Redux Auth State Structure                    │
└─────────────────────────────────────────────────────────────────┘

authSlice: {
  user: {
    _id: "...",
    name: "John Doe",
    email: "john@example.com",
    phone: "+91 9876543210",
    isAdmin: false,
    token: "eyJhbGciOiJIUzI1NiIs...",
    avatar: "https://ui-avatars.com/api/?name=John+Doe"
  },
  isAuthenticated: true,
  token: "eyJhbGciOiJIUzI1NiIs...",
  loading: false,
  error: null
}

Synced with localStorage:
- localStorage.user (JSON string)
- localStorage.token (string)
```

This architecture ensures:
✅ Security at every layer
✅ Smooth user experience
✅ Data integrity
✅ Scalability
✅ Maintainability
