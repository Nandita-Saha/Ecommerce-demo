# Environment Configuration Example
# Copy this file to .env and fill in your actual values

# Application Environment
NODE_ENV=development

# MongoDB Connection
# For local development, use:
MONGO_URI=mongodb://127.0.0.1:27017/ecommerce

# For production (MongoDB Atlas), use:
# MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/ecommerce?retryWrites=true&w=majority

# JWT Secret (use a long, random string)
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here

# Server Port
PORT=5000

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================
# 
# For Render deployment (FREE FOREVER ✅):
# 1. Create MongoDB Atlas account (free tier)
# 2. Get your connection string from Atlas
# 3. Generate a NEW JWT_SECRET for production
# 4. Add these variables to Render dashboard
# 5. Set NODE_ENV=production
# 6. See RENDER_DEPLOYMENT.md for detailed guide
#
# For Railway deployment (Free trial - $5 credit):
# 1. Create MongoDB Atlas account (free tier)
# 2. Get your connection string from Atlas
# 3. Generate a NEW JWT_SECRET for production
# 4. Add these variables to Railway dashboard
# 5. Set NODE_ENV=production
# 6. See RAILWAY_DEPLOYMENT.md for detailed guide
#
# Never commit the actual .env file to Git!
# ============================================
